import { AsyncvalidationCompleteComponent } from 'src/assets/examples/dynamic-forms/asyncvalidation/asyncvalidation-complete.component';

export const ASYNCVALIDATION_COMPONENT_EXAMPLE_CONSTANT: { [key: string]: any } = {
    validators:
    {
        complete:AsyncvalidationCompleteComponent 
    }
}